// Shared spacing tokens for the Home dashboard, so every card and grid gap uses the same scale.

/** Inner padding for every card on the page (SectionCard, ProfileHeroCard, StatTile, CurrentPeriodBanner). */
export const CARD_PADDING = 2.5;

/** Gap between cards, both within a Stack and between Grid items. */
export const SECTION_GAP = 3;

/** Horizontal/vertical padding for a clickable row inside a card (NeedsActionList, QuickActions). */
export const ROW_PADDING_X = 1.5;
export const ROW_PADDING_Y = 1.25;
